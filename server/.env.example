# Resend API key (get one free at https://resend.com — no password needed)
# This replaces SMTP/password auth. The key is revocable and purpose-limited.
RESEND_API_KEY=re_LTQqTgx5_8iWqsacrLBpaPBNW9XbYH8SZ

# "From" address — use onboarding@resend.dev (free, no setup) OR verify
# your own email/domain in the Resend dashboard to send as your Gmail address.
FROM_EMAIL=MIBJ Contact <onboarding@resend.dev>

# Where contact-form submissions are sent
CONTACT_EMAIL=medislambenjaballah1@gmail.com

# Server port
PORT=3000
